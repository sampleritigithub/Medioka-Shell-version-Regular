/****Copyrights:****\
 *Omar Ismail 2022 *
\*******************/
int bar() {
  while (true) {
    /********************\
     *Window bar        *
     *Window Options (X)*
    \********************/
    window = "Regular Window";
    state = window;
    if (state == window) {
      
    }
    else{
      break; 
    }
  }
}
int rmen() {
  while (true) {
     /***************\
     *Window bar (X)*
     *Window Options*
    \****************/
    window = "Regular Window";
    state = window;
    if (state == window) {
      
    }
    else{
      break; 
    }
  }
}
